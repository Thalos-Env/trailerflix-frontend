import { Story, Meta } from '@storybook/react'
import MenuHeader from '.'

export default {
  title: 'MenuHeader',
  component: MenuHeader,
} as Meta

export const Default: Story = () => <MenuHeader />
